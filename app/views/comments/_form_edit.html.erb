<div class="container-fluid my-4">
  <div class="row">
    <div class="col-md-2 mx-auto mt-1">
    </div>
    <div class="col-md-8 p-0">
      <div class="m-5">
        <%= image_tag gravatar_url(current_user.email, 180), alt: "", class:"rounded-circle mx-auto d-block" %>
        <h3 class="text-center m-2"><strong><%=current_user.username%></strong></h3>
      </div>

      <%= form_for ([@post, @comment]) do |f| %>

      <!--
      collection.build(attributes = {}, …) Returns one or more new objects of the collection type that have been instantiated with attributes and linked to this object
      through a foreign key, but have not yet been saved. Note: This only works if an associated object already exists, not if it‘s nil!
      -->

      <!-- <%= render "posts/error_comment", :target => @comment %> -->

      <%= f.text_area :content, class:"form-control", placeholder: "Escribir comentario..." %>
      <br>
      <%= f.submit "Editar!", class:"btn btn-success float-right"%>
      <% end %>
      <%= link_to  "Mejor después...", post_path(@post), class:"btn btn-outline-danger" %>

    </div>
    <div class="col-md-2 mx-auto mt-1">
    </div>
  </div>
</div>